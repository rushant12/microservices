DefaultAzureCredential credential = new DefaultAzureCredentialBuilder()
    .excludeEnvironmentCredential() // disable if not using env vars
    .excludeIntelliJCredential()    // disable if not using IntelliJ auth
    .excludeSharedTokenCacheCredential()
    .build();
